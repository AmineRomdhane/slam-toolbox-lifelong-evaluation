# V0/R1 ideal observed reference

`decisions.json` freezes input handling. `input_inspection.json` preserves the
original inspection, whose pending decision is superseded by decisions.json.
Generator: `experiments/reference_v0_r1`. Generated grids, ray evidence and
validation reports remain local; generation attempts are retained separately.
