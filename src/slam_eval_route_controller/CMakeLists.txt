cmake_minimum_required(VERSION 3.16)
project(slam_eval_route_controller)
find_package(ament_cmake REQUIRED)
install(PROGRAMS scripts/controller.py DESTINATION lib/${PROJECT_NAME} RENAME route_controller)
install(FILES scripts/control_core.py DESTINATION lib/${PROJECT_NAME})
install(DIRECTORY routes DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
 enable_testing()
 add_test(NAME control_core COMMAND /usr/bin/python3 ${CMAKE_CURRENT_SOURCE_DIR}/test/test_core.py)
endif()
ament_package()
